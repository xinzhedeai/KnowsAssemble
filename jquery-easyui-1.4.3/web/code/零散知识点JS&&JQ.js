/**
 * 当json数据进行格式转化的时候如果方生问题，优先考虑是否数据中含有空格。
 * 可以在表单提交的时候，对表单字段的值进行去除空格处理
 */
JSON.stringify(resObj.menu);//将json对象转化为json字符串
$.parseJSON()//jquery的将json字符串转化为json对象的方法

/**
 * jquery的inArray（）方法
 * 判断数组2中，是否有1元素，如果没有则返回-1
 */

new String().toLowerCase()//字符串的变小写方法
delete obj.attr1;//删除对象中的某个属性
var a = undefined;//变量初始化的时候，进行不用null或者是''，最好使用undefined

e.preventDefault();//禁止浏览器默认事件。
